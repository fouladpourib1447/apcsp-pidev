#include <stdio.h>

int arrayAdd(int s, int n)
{
  int arr[s];
  int s = 100;
  n = 1;

  for (float i = 0; i<100; i++)
{
    arr[i] = i * i;
}
  printf("Array: %d\n", s, n);
}

int main()
{
  int a = 1;
  printf("Array: %d\n", a);
  int r;
  r = arrayAdd(100, 1);
  printf("array: s=%d, n=%d\n", s, n);
}

